spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flat_rent
    username: postgres
    password: 123456
    driver: org.postgresql.Driver

  jpa:
    hibernate:
      ssl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
