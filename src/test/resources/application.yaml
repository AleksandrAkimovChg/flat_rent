server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flat_rent?currentSchema=test
    username: postgres
    password: 123456
    driver: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    org:
      hibernate:
        orm:
          jdbc:
            bind: INFO
    com:
      javaacademy:
        flat_rent: INFO
